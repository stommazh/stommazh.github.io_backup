import typhography from 'windicss/plugin/typography';
export default {
	darkMode: 'class',
	plugins: [
		typhography,
	],
	theme: {
		colors: {
			black: {
				dim: '#ccc',
				DEFAULT: '#000'
			}
		},
	},
}
